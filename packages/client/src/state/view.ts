// Graph data arrives via WebSocket (view_snapshot / hierarchy_snapshot).
// There is no REST reload path — this module is intentionally empty.
// Kept so that any existing imports compile without changes.
export {}
