# GraphCoder — SQLite caches stay local, annotations get committed.
temporal.sqlite
temporal.sqlite-wal
temporal.sqlite-shm

# Test fixture — annotations and kinds created by e2e tests are transient.
annotations/
annotation-kinds.json
